# Infrastructure

Ce dossier contient les fichiers d'aide pour preparer la mise en production.

- `aws/` : exemples et notes pour deploiement AWS.
- Le `docker-compose.yml` principal reste a la racine pour garder les commandes Docker simples en local.
